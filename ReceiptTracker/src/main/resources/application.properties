# default application properties for ReceiptTracker application
spring.application.name=ReceiptTracker

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/receipttracker
spring.datasource.username=root
spring.datasource.password=receipttracker4800
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# App
server.port=8080